---
phase: 04-local-repository-input
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/github-researcher/lib/intake/types.ts
  - skills/github-researcher/lib/intake/error-codes.ts
  - skills/github-researcher/lib/intake/preflight.ts
  - skills/github-researcher/lib/intake/local-resolver.ts
  - tests/intake/local-preflight.spec.ts
  - tests/intake/preflight.spec.ts
autonomous: true
requirements:
  - INPT-03
user_setup: []
must_haves:
  truths:
    - "Local path intake supports absolute, relative, and ~ paths and normalizes to deterministic absolute real paths."
    - "Local intake returns structured, non-retryable failures for deterministic path/git errors with actionable remediation."
    - "When a path is inside a git repository, intake resolves to repo root and emits canonical metadata with local visibility semantics."
  artifacts:
    - path: skills/github-researcher/lib/intake/types.ts
      provides: local_path input and local metadata contract extensions compatible with existing intake result envelopes
      min_lines: 70
      exports:
        - InputType
        - IntakeSuccess
        - IntakeFailure
    - path: skills/github-researcher/lib/intake/error-codes.ts
      provides: local-specific error code taxonomy and class mapping for deterministic local intake failures
      min_lines: 40
      exports:
        - ERROR_CODE
        - ERROR_CLASS
    - path: skills/github-researcher/lib/intake/local-resolver.ts
      provides: local path normalization, git root/branch resolution, canonical identity derivation, and structured failure emission
      min_lines: 130
      exports:
        - resolveLocalRepositoryTarget
    - path: skills/github-researcher/lib/intake/preflight.ts
      provides: integrated local resolver branch in canonical intake preflight flow without URL/search regression
      min_lines: 120
      exports:
        - runRepositoryPreflight
    - path: tests/intake/local-preflight.spec.ts
      provides: local intake unit coverage for normalization, error taxonomy, repo root resolution, and canonical fallback behavior
      min_lines: 100
    - path: tests/intake/preflight.spec.ts
      provides: no-regression coverage proving URL/search/local intake mode compatibility
      min_lines: 90
  key_links:
    - from: skills/github-researcher/lib/intake/preflight.ts
      to: skills/github-researcher/lib/intake/local-resolver.ts
      via: preflight dispatches local inputs to deterministic local resolver and maps results into shared intake contracts
      pattern: local_path|resolveLocalRepositoryTarget|IntakeResult
    - from: skills/github-researcher/lib/intake/local-resolver.ts
      to: skills/github-researcher/lib/intake/error-codes.ts
      via: local resolver maps path/git failure modes to local-specific error codes and non-retryable classifications
      pattern: PATH_NOT_FOUND|PATH_NOT_READABLE|NON_GIT_DIRECTORY|GIT_METADATA_UNRESOLVED
    - from: tests/intake/local-preflight.spec.ts
      to: skills/github-researcher/lib/intake/local-resolver.ts
      via: tests prove deterministic normalization/root/canonical rules and local fallback identity semantics
      pattern: normalize|repo_root|canonical_id|local:
---

<objective>
Implement local repository intake foundation for Phase 04 so runs can start from local paths using strict contracts, deterministic local error taxonomy, canonical metadata resolution, and test-backed preflight integration.

Purpose: Satisfy INPT-03 intake behavior while preserving existing URL/search intake behavior and reliability assumptions.
Output: Local intake contracts + error codes + resolver + preflight integration with focused unit/regression tests.
</objective>

<execution_context>
@/Users/eddy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/eddy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-local-repository-input/04-CONTEXT.md
@.planning/phases/04-local-repository-input/04-RESEARCH.md
@.planning/phases/04-local-repository-input/04-VALIDATION.md
@skills/github-researcher/lib/intake/types.ts
@skills/github-researcher/lib/intake/error-codes.ts
@skills/github-researcher/lib/intake/preflight.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend intake contracts and local error taxonomy</name>
  <files>skills/github-researcher/lib/intake/types.ts, skills/github-researcher/lib/intake/error-codes.ts, tests/intake/local-preflight.spec.ts</files>
  <action>Add local input contract support (`input_type=local_path`) and local repository metadata fields needed by downstream run context. Add local-specific error codes and deterministic class mapping for path/readability/non-git/git-metadata failures, and seed unit assertions for these contracts before resolver integration.</action>
  <verify>rg -n "local_path|normalized_path|repo_root|canonical_id|visibility|PATH_NOT_FOUND|PATH_NOT_READABLE|NON_GIT_DIRECTORY|GIT_METADATA_UNRESOLVED" skills/github-researcher/lib/intake/types.ts skills/github-researcher/lib/intake/error-codes.ts tests/intake/local-preflight.spec.ts</verify>
  <automated>npm exec --yes vitest run tests/intake/local-preflight.spec.ts</automated>
  <done>Local intake contract and deterministic error taxonomy are explicit, typed, and ready for preflight/local resolver integration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement local preflight resolver for path normalization and git metadata resolution</name>
  <files>skills/github-researcher/lib/intake/local-resolver.ts, skills/github-researcher/lib/intake/preflight.ts, tests/intake/local-preflight.spec.ts</files>
  <action>Implement local resolver flow: normalize path (`~`, relative, realpath), validate readable path, resolve git repo root, derive branch (HEAD fallback `main`), derive canonical identity from remote or `local:&lt;repo-name&gt;`, and return structured success/failure payloads. Wire resolver into `runRepositoryPreflight` local path branch with deterministic, non-retryable local failures.</action>
  <verify>rg -n "resolveLocalRepositoryTarget|realpath|rev-parse --show-toplevel|symbolic-ref --short HEAD|canonical_id|local:|runRepositoryPreflight" skills/github-researcher/lib/intake/local-resolver.ts skills/github-researcher/lib/intake/preflight.ts</verify>
  <automated>npm exec --yes vitest run tests/intake/local-preflight.spec.ts</automated>
  <done>Local path inputs deterministically resolve into canonical intake success/failure envelopes compatible with existing preflight semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage across intake modes</name>
  <files>tests/intake/preflight.spec.ts, tests/intake/local-preflight.spec.ts</files>
  <action>Expand preflight test matrix to verify no regressions for existing URL/search mode behavior and confirm local mode interop, including subdirectory-to-repo-root resolution and structured remediation messaging for deterministic local failures.</action>
  <verify>rg -n "local_path|github_url|search|subdirectory|repo_root|suggestions|non-retryable" tests/intake/preflight.spec.ts tests/intake/local-preflight.spec.ts</verify>
  <automated>npm exec --yes vitest run tests/intake/preflight.spec.ts tests/intake/local-preflight.spec.ts</automated>
  <done>Intake preflight behavior is verified across all currently supported input modes without contract regression.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm exec --yes vitest run tests/intake/local-preflight.spec.ts` passes
- [ ] `npm exec --yes vitest run tests/intake/preflight.spec.ts` passes
- [ ] Local path normalization is deterministic for relative and `~` inputs
- [ ] Nonexistent/non-readable/non-git inputs produce structured non-retryable local error codes
- [ ] Subdirectory inputs resolve to repository root and emit canonical local metadata
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- INPT-03 local intake contracts, error taxonomy, and preflight resolver behavior are implemented with no regressions to existing intake modes
- Phase 04 wave-2 reliability integration can consume local preflight output without contract changes
</success_criteria>

<output>
After completion, create `.planning/phases/04-local-repository-input/04-01-SUMMARY.md`
</output>
