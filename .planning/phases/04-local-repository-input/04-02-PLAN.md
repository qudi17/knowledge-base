---
phase: 04-local-repository-input
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - skills/github-researcher/lib/reliability/types.ts
  - skills/github-researcher/lib/reliability/orchestrator.ts
  - skills/github-researcher/lib/intake/fingerprint.ts
  - tests/reliability/local-input-reliability.spec.ts
  - tests/intake/preflight.spec.ts
autonomous: true
requirements:
  - INPT-03
user_setup: []
must_haves:
  truths:
    - "Successful local intake uses the same reliability orchestration and stage lifecycle semantics as existing input modes."
    - "Deterministic local intake failures terminate promptly with non-retryable terminal reasoning and without transient retry churn."
    - "Local run fingerprint and resume behavior remain stable across reruns for equivalent normalized local targets."
  artifacts:
    - path: skills/github-researcher/lib/reliability/types.ts
      provides: run context typing additions for local input metadata and terminal reasoning compatibility
      min_lines: 50
      exports:
        - ReliabilityRunInput
        - ReliabilityRunState
    - path: skills/github-researcher/lib/intake/fingerprint.ts
      provides: stable fingerprint derivation that includes canonical local identity/path semantics without mode drift
      min_lines: 60
      exports:
        - buildRunInputFingerprint
    - path: skills/github-researcher/lib/reliability/orchestrator.ts
      provides: local input compatibility in stage execution, terminal classification, checkpointing, and resume logic
      min_lines: 140
      exports:
        - runWithReliability
    - path: tests/reliability/local-input-reliability.spec.ts
      provides: integration coverage for local success progression, deterministic failure termination, and resume behavior
      min_lines: 110
    - path: tests/intake/preflight.spec.ts
      provides: compatibility assertions ensuring preflight output shape remains consumable by reliability entrypoints
      min_lines: 100
  key_links:
    - from: skills/github-researcher/lib/reliability/orchestrator.ts
      to: skills/github-researcher/lib/intake/preflight.ts
      via: orchestrator consumes local preflight output through the same stage boundary used by URL/search modes
      pattern: runRepositoryPreflight|input_type|local_path|terminal_reason
    - from: skills/github-researcher/lib/intake/fingerprint.ts
      to: skills/github-researcher/lib/reliability/orchestrator.ts
      via: local canonical identity and normalized path feed stable run fingerprint and checkpoint resume matching
      pattern: fingerprint|canonical_id|normalized_path|resume
    - from: tests/reliability/local-input-reliability.spec.ts
      to: skills/github-researcher/lib/reliability/orchestrator.ts
      via: tests assert deterministic local-failure no-retry behavior and successful resume-safe stage progression
      pattern: non-retryable|retry_count|checkpoint|resume
---

<objective>
Integrate local repository intake with reliability orchestration, checkpoint/fingerprint semantics, and resume compatibility so local targets run unattended with deterministic failure behavior and stable rerun identity.

Purpose: Complete INPT-03 end-to-end execution compatibility beyond intake contracts by proving reliable launch/resume behavior.
Output: Reliability/fingerprint integration plus integration tests for local success/failure/resume pathways.
</objective>

<execution_context>
@/Users/eddy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/eddy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-local-repository-input/04-CONTEXT.md
@.planning/phases/04-local-repository-input/04-RESEARCH.md
@.planning/phases/04-local-repository-input/04-VALIDATION.md
@.planning/phases/04-local-repository-input/04-01-PLAN.md
@skills/github-researcher/lib/reliability/orchestrator.ts
@skills/github-researcher/lib/reliability/types.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Integrate local intake outputs into reliability lifecycle semantics</name>
  <files>skills/github-researcher/lib/reliability/types.ts, skills/github-researcher/lib/reliability/orchestrator.ts, tests/reliability/local-input-reliability.spec.ts</files>
  <action>Update reliability input/state typing and orchestration path so local preflight successes launch with the same stage transitions as existing modes, while deterministic local preflight failures terminate as non-retryable outcomes without entering transient retry loops.</action>
  <verify>rg -n "local_path|terminal_reason|non-retryable|retry|stage|runWithReliability" skills/github-researcher/lib/reliability/types.ts skills/github-researcher/lib/reliability/orchestrator.ts tests/reliability/local-input-reliability.spec.ts</verify>
  <automated>npm exec --yes vitest run tests/reliability/local-input-reliability.spec.ts</automated>
  <done>Reliability behavior for local inputs matches unattended execution expectations and deterministic local failures do not churn retries.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure local fingerprint and resume compatibility are stable</name>
  <files>skills/github-researcher/lib/intake/fingerprint.ts, skills/github-researcher/lib/reliability/orchestrator.ts, tests/reliability/local-input-reliability.spec.ts</files>
  <action>Implement/update run fingerprint composition to include stable canonical local identity and normalized path semantics so equivalent local targets map to the same run lineage. Add resume assertions proving completed stages are reused and equivalent local preflight discovery does not rerun unnecessarily.</action>
  <verify>rg -n "buildRunInputFingerprint|canonical_id|normalized_path|checkpoint|resume|run_id" skills/github-researcher/lib/intake/fingerprint.ts skills/github-researcher/lib/reliability/orchestrator.ts tests/reliability/local-input-reliability.spec.ts</verify>
  <automated>npm exec --yes vitest run tests/reliability/local-input-reliability.spec.ts</automated>
  <done>Local run fingerprinting is deterministic and resume behavior is stable across equivalent reruns.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end local reliability integration coverage</name>
  <files>tests/reliability/local-input-reliability.spec.ts, tests/intake/preflight.spec.ts</files>
  <action>Expand integration tests to cover local happy-path launch progression, deterministic local-failure terminal path, and interrupted-run resume path reusing checkpoints. Confirm preflight output remains compatible with reliability orchestration contracts.</action>
  <verify>rg -n "local input|happy path|terminal|non-retryable|resume|checkpoint|preflight" tests/reliability/local-input-reliability.spec.ts tests/intake/preflight.spec.ts</verify>
  <automated>npm exec --yes vitest run tests/reliability/local-input-reliability.spec.ts tests/intake/preflight.spec.ts</automated>
  <done>INPT-03 reliability integration is proven across success, deterministic failure, and resume scenarios with automated evidence.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm exec --yes vitest run tests/reliability/local-input-reliability.spec.ts` passes
- [ ] `npm exec --yes vitest run tests/intake/preflight.spec.ts` passes
- [ ] Local deterministic intake failures terminate without transient retry loops
- [ ] Local success path proceeds through existing reliability stages without mode-specific divergence
- [ ] Equivalent local inputs produce stable fingerprints and resume from checkpoints predictably
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- INPT-03 is satisfied end-to-end from local intake through reliability execution and resume compatibility
- Local input mode behaves as a first-class launch path without regressions to existing modes
</success_criteria>

<output>
After completion, create `.planning/phases/04-local-repository-input/04-02-SUMMARY.md`
</output>
