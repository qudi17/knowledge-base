---
phase: 01-repository-intake-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/github-researcher/lib/intake/github-client.ts
  - skills/github-researcher/lib/intake/retry.ts
  - skills/github-researcher/lib/intake/error-codes.ts
  - skills/github-researcher/lib/intake/http-types.ts
autonomous: true
requirements:
  - INPT-04
user_setup: []
must_haves:
  truths:
    - "Repository existence and canonical identity are resolved from GitHub API before analysis starts."
    - "Canonical repository identity from API overrides user input when rename or transfer is detected."
    - "Transient GitHub/API failures are retried with bounded backoff and then classified predictably."
    - "Failures include structured error object fields and a human-readable summary string for unattended diagnostics."
  artifacts:
    - path: skills/github-researcher/lib/intake/github-client.ts
      provides: canonical repository metadata lookup and default branch resolution
      min_lines: 50
      exports:
        - fetchCanonicalRepository
    - path: skills/github-researcher/lib/intake/retry.ts
      provides: bounded exponential backoff helper for transient failures
      min_lines: 25
      exports:
        - retryTransient
    - path: skills/github-researcher/lib/intake/error-codes.ts
      provides: stable preflight error-code catalog and retryability mapping
      min_lines: 20
      contains: TRANSIENT_ERROR
  key_links:
    - from: skills/github-researcher/lib/intake/github-client.ts
      to: skills/github-researcher/lib/intake/retry.ts
      via: GitHub API operations wrapped in bounded retry policy
      pattern: retryTransient
    - from: skills/github-researcher/lib/intake/github-client.ts
      to: skills/github-researcher/lib/intake/error-codes.ts
      via: API response classes mapped to stable error codes
      pattern: error_code|TRANSIENT_ERROR|PERMISSION_OR_UNSUPPORTED
---

<objective>
Implement GitHub preflight client primitives for canonical identity and default-branch resolution.

Purpose: Ensure phase intake can validate repository existence and emit canonical run identity regardless of input aliases/renames.
Output: Typed GitHub client module, retry helper, and error-code mapping primitives consumed by the intake orchestrator.
</objective>

<execution_context>
@/Users/eddy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/eddy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-intake-foundation/01-CONTEXT.md
@.planning/phases/01-repository-intake-foundation/01-RESEARCH.md
@skills/github-researcher/SKILL.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create stable intake error-code and HTTP response contracts</name>
  <files>skills/github-researcher/lib/intake/error-codes.ts, skills/github-researcher/lib/intake/http-types.ts</files>
  <action>Define explicit error codes for invalid format, unsupported/private repository, not found, rate limit, and transient upstream failures. Add response contracts used by the GitHub client so status handling remains deterministic and testable.</action>
  <verify>test -f skills/github-researcher/lib/intake/error-codes.ts && test -f skills/github-researcher/lib/intake/http-types.ts</verify>
  <automated>pnpm vitest run tests/intake/github-client.spec.ts</automated>
  <done>Error-code catalog and HTTP contract types are present and consumable by intake client logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded retry helper for transient GitHub failures</name>
  <files>skills/github-researcher/lib/intake/retry.ts</files>
  <action>Implement retry utility with explicit max attempts and bounded exponential backoff. Restrict retries to transient classes (rate limits, network faults, 5xx). Ensure non-retryable classes fail immediately and preserve diagnostics for caller reporting.</action>
  <verify>rg -n "maxAttempts|retryTransient|TRANSIENT" skills/github-researcher/lib/intake/retry.ts</verify>
  <automated>pnpm vitest run tests/intake/github-client.spec.ts</automated>
  <done>Retry helper enforces bounded attempts, preserves terminal error context, and can be reused across preflight HTTP calls.</done>
</task>

<task type="auto">
  <name>Task 3: Implement GitHub canonical repository resolver client</name>
  <files>skills/github-researcher/lib/intake/github-client.ts</files>
  <action>Implement API-backed repository lookup that resolves canonical owner/repo and default branch, detects identity changes (rename/transfer/casing), and returns typed structured output. Map private-repo and permission issues to PERMISSION_OR_UNSUPPORTED for v1 and classify transient failures consistently. Ensure all failure returns include both structured fields and a concise human-readable summary string.</action>
  <verify>rg -n "fetchCanonicalRepository|default_branch|canonical|PERMISSION_OR_UNSUPPORTED|TRANSIENT_ERROR" skills/github-researcher/lib/intake/github-client.ts</verify>
  <automated>pnpm vitest run tests/intake/github-client.spec.ts</automated>
  <done>Client returns canonical repository metadata on success and stable structured failures (error_class/error_code/details + summary text) on unsupported, missing, and transient failure paths.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest run tests/intake/github-client.spec.ts` smoke check passes after each task edit
- [ ] `pnpm vitest run tests/intake/github-client.spec.ts` passes
- [ ] Canonical identity override is exercised for rename/transfer scenario
- [ ] Default branch resolution is included in successful output contract
- [ ] Transient failures are retried within bounds and then classified with preserved diagnostics and human-readable summary
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- GitHub preflight primitives are ready for parser/normalizer pipeline integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-intake-foundation/01-02-SUMMARY.md`
</output>
