---
phase: 01-repository-intake-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - skills/github-researcher/lib/intake/preflight.ts
  - skills/github-researcher/lib/intake/index.ts
  - tests/intake/parser-normalizer.spec.ts
  - tests/intake/validator.spec.ts
  - tests/intake/github-client.spec.ts
  - tests/intake/preflight.spec.ts
autonomous: true
requirements:
  - INPT-01
  - INPT-04
user_setup: []
must_haves:
  truths:
    - "A run can start from GitHub URL input and always reaches a canonical preflight success or structured preflight failure."
    - "Preflight executes in fixed order: syntax parse, safe normalization, policy validation, remote canonical resolution."
    - "All accepted targets emit canonical owner/repo plus default branch used for downstream run identity and caching."
    - "Run logs/artifacts include both display identity and canonical identity for traceable repository targeting."
    - "Each failure emits a structured error object and a human-readable summary."
  artifacts:
    - path: skills/github-researcher/lib/intake/preflight.ts
      provides: end-to-end intake orchestration and success/failure contract assembly
      min_lines: 70
      exports:
        - runRepositoryPreflight
    - path: tests/intake/preflight.spec.ts
      provides: integration coverage for ordered pipeline and terminal output contract
      min_lines: 60
    - path: tests/intake/parser-normalizer.spec.ts
      provides: coverage for accepted input forms and normalization provenance
      min_lines: 40
  key_links:
    - from: skills/github-researcher/lib/intake/preflight.ts
      to: skills/github-researcher/lib/intake/parser.ts
      via: first-stage syntax parsing in fixed pipeline
      pattern: parseRepositoryTarget
    - from: skills/github-researcher/lib/intake/preflight.ts
      to: skills/github-researcher/lib/intake/github-client.ts
      via: canonical identity and default branch resolution before success output
      pattern: fetchCanonicalRepository
    - from: tests/intake/preflight.spec.ts
      to: skills/github-researcher/lib/intake/preflight.ts
      via: assertion of strict stage ordering and failure taxonomy
      pattern: runRepositoryPreflight
    - from: skills/github-researcher/lib/intake/preflight.ts
      to: skills/github-researcher/references/report-template.md
      via: preflight output fields carried into report/run artifacts for display and canonical identity traceability
      pattern: display|canonical
---

<objective>
Integrate Phase 1 intake modules into a single deterministic preflight pipeline and back it with Nyquist-aligned tests.

Purpose: Deliver executable, auditable proof that INPT-01 and INPT-04 are fully satisfied before analysis starts.
Output: Orchestrated preflight entrypoint, exported intake surface, and comprehensive test suite for parser/validator/client/preflight behavior.
</objective>

<execution_context>
@/Users/eddy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/eddy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-intake-foundation/01-RESEARCH.md
@.planning/phases/01-repository-intake-foundation/01-VALIDATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build preflight orchestrator with strict stage ordering</name>
  <files>skills/github-researcher/lib/intake/preflight.ts, skills/github-researcher/lib/intake/index.ts</files>
  <action>Implement runRepositoryPreflight to enforce the fixed execution order from research: parse -> normalize -> validate -> GitHub canonical resolution -> final output assembly. Include explicit normalization visibility and input->canonical identity mapping in success payload, and include both display identity and canonical identity in emitted run-log/artifact payload fields. Emit failures as structured classed objects plus a concise human-readable summary.</action>
  <verify>rg -n "runRepositoryPreflight|parseRepositoryTarget|normalizeRepositoryTarget|validateRepositoryCandidate|fetchCanonicalRepository" skills/github-researcher/lib/intake/preflight.ts</verify>
  <automated>pnpm vitest run tests/intake/preflight.spec.ts</automated>
  <done>Preflight returns deterministic success/failure contracts, includes display+canonical identity in logs/artifacts, and exports a single intake entrypoint for downstream workflow integration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement unit tests for parser, normalizer, validator, and GitHub client contracts</name>
  <files>tests/intake/parser-normalizer.spec.ts, tests/intake/validator.spec.ts, tests/intake/github-client.spec.ts</files>
  <action>Add test cases covering all accepted input forms, malformed targets, unsupported host rejection, no-typo-autocorrect behavior, canonical rename/transfer identity override, default branch propagation, private/unsupported classification, and transient retry behavior boundaries.</action>
  <verify>pnpm vitest run tests/intake/parser-normalizer.spec.ts tests/intake/validator.spec.ts tests/intake/github-client.spec.ts</verify>
  <automated>pnpm vitest run tests/intake/parser-normalizer.spec.ts</automated>
  <done>Unit/contract tests provide direct acceptance coverage for INPT-01 and INPT-04 edge behaviors defined in context/research.</done>
</task>

<task type="auto">
  <name>Task 3: Add preflight integration tests and validate phase acceptance contract</name>
  <files>tests/intake/preflight.spec.ts</files>
  <action>Create mocked-HTTP integration tests that verify strict stage ordering, terminal success object shape (display identity + canonical_id + default_branch + normalizations_applied), and terminal failure object shape (error_class/error_code/message/details/retryable/summary). Include scenarios for malformed input, unsupported host, not-found repository, private repository, transient upstream failures, and rename/transfer canonical mapping.</action>
  <verify>pnpm vitest run tests/intake/preflight.spec.ts</verify>
  <automated>pnpm vitest run tests/intake/preflight.spec.ts</automated>
  <done>Integration suite proves phase goal behaviors, display+canonical identity traceability, and structured+summary failure taxonomy end-to-end for repository intake preflight.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest run tests/intake/parser-normalizer.spec.ts` smoke check passes after each task edit
- [ ] `pnpm vitest run tests/intake/parser-normalizer.spec.ts tests/intake/validator.spec.ts` passes
- [ ] `pnpm vitest run tests/intake/github-client.spec.ts tests/intake/preflight.spec.ts` passes
- [ ] `pnpm vitest run tests/intake/*.spec.ts tests/intake/**/*.spec.ts` passes
- [ ] Success payload includes display identity, canonical repository ID, and default branch; failure payload includes structured class/code/diagnostics and summary
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 goal is demonstrably satisfied by tests and preflight output contracts
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-intake-foundation/01-03-SUMMARY.md`
</output>
