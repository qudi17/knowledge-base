---
phase: 01-repository-intake-foundation
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - tests/intake/preflight.spec.ts
  - tests/intake/github-client.spec.ts
  - .planning/phases/01-repository-intake-foundation/01-VALIDATION.md
  - .planning/phases/01-repository-intake-foundation/01-04-SUMMARY.md
autonomous: true
requirements:
  - INPT-01
  - INPT-04
user_setup: []
must_haves:
  truths:
    - "Preflight integration tests explicitly cover the missing 01-VERIFICATION scenarios: not found, private repository, transient upstream failure, rename/transfer canonical mapping, and fixed stage ordering."
    - "GitHub client tests explicitly prove bounded retry behavior (attempt ceiling) and terminal transient classification for canonical fetch operations."
    - "Gap-closure verification commands are executable in the current environment without pnpm and are documented in durable phase validation artifacts."
  artifacts:
    - path: tests/intake/preflight.spec.ts
      provides: gap-closure integration scenario matrix and stage-order assertions for runRepositoryPreflight
      min_lines: 80
    - path: tests/intake/github-client.spec.ts
      provides: retry-boundary and transient-classification assertions for fetchCanonicalRepository
      min_lines: 80
    - path: .planning/phases/01-repository-intake-foundation/01-VALIDATION.md
      provides: durable Phase 1 validation contract updated with non-pnpm executable commands for gap-closure execution
      min_lines: 60
  key_links:
    - from: tests/intake/preflight.spec.ts
      to: skills/github-researcher/lib/intake/preflight.ts
      via: scenario assertions for strict parse->normalize->validate->canonical pipeline order and terminal taxonomy behavior
      pattern: runRepositoryPreflight|parse|normalize|validate|canonical
    - from: tests/intake/github-client.spec.ts
      to: skills/github-researcher/lib/intake/github-client.ts
      via: retry-attempt and transient classification assertions against canonical fetch behavior
      pattern: fetchCanonicalRepository|retry|TRANSIENT_ERROR|attempt
    - from: .planning/phases/01-repository-intake-foundation/01-VALIDATION.md
      to: .planning/phases/01-repository-intake-foundation/01-VERIFICATION.md
      via: validation command profile explicitly addresses environment gap and verification finding #3
      pattern: pnpm|npm exec|gap closure|verification
gap_closure: true
---

<objective>
Close verification gaps for Phase 1 by adding missing scenario coverage and replacing environment-fragile verification commands with practical checks that run without pnpm.
</objective>

<execution_context>
@/Users/eddy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/eddy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repository-intake-foundation/01-CONTEXT.md
@.planning/phases/01-repository-intake-foundation/01-RESEARCH.md
@.planning/phases/01-repository-intake-foundation/01-VERIFICATION.md
@.planning/phases/01-repository-intake-foundation/01-03-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Expand preflight integration scenarios to match planned matrix</name>
  <files>tests/intake/preflight.spec.ts</files>
  <action>Add missing preflight cases required by verification findings: repository not found, private repository classification, transient upstream failure handling, rename/transfer canonical mapping, and explicit parse->normalize->validate->canonical ordering assertions.</action>
  <verify>rg -n "not found|private|transient|rename|transfer|order|parse|normalize|validate|canonical" tests/intake/preflight.spec.ts</verify>
  <automated>npm exec --yes vitest run tests/intake/preflight.spec.ts || rg -n "not found|private|transient|rename|transfer|order|parse|normalize|validate|canonical" tests/intake/preflight.spec.ts</automated>
  <done>Preflight integration suite covers all scenarios listed as missing in 01-VERIFICATION gap 1, including strict pipeline ordering assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit bounded-retry behavior assertions for GitHub client</name>
  <files>tests/intake/github-client.spec.ts</files>
  <action>Add tests that assert retry attempt ceilings and terminal transient classification behavior for fetchCanonicalRepository, including checks that non-retryable failures do not consume retry budget.</action>
  <verify>rg -n "retry|attempt|max|bounded|TRANSIENT_ERROR|non-retryable|fetchCanonicalRepository" tests/intake/github-client.spec.ts</verify>
  <automated>npm exec --yes vitest run tests/intake/github-client.spec.ts || rg -n "retry|attempt|max|bounded|TRANSIENT_ERROR|non-retryable|fetchCanonicalRepository" tests/intake/github-client.spec.ts</automated>
  <done>GitHub client tests explicitly prove retry bounds and classification outcomes required by 01-VERIFICATION gap 2.</done>
</task>

<task type="auto">
  <name>Task 3: Update durable validation contract for non-pnpm execution path</name>
  <files>.planning/phases/01-repository-intake-foundation/01-VALIDATION.md</files>
  <action>Update Phase 1 validation strategy to include a gap-closure command profile that does not require pnpm: file-existence checks, targeted rg assertions for scenario/retry coverage, environment checks (`node -v`, `npm -v`), and optional npm-exec vitest commands when tooling is available.</action>
  <verify>rg -n "gap|closure|npm exec|node -v|npm -v|preflight\.spec|github-client\.spec" .planning/phases/01-repository-intake-foundation/01-VALIDATION.md</verify>
  <automated>test -f .planning/phases/01-repository-intake-foundation/01-VALIDATION.md && rg -n "gap|closure|npm exec|node -v|npm -v|preflight\.spec|github-client\.spec" .planning/phases/01-repository-intake-foundation/01-VALIDATION.md</automated>
  <done>01-VALIDATION.md persistently documents executable gap-closure verification steps for this environment, directly addressing 01-VERIFICATION gap 3.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f tests/intake/preflight.spec.ts && test -f tests/intake/github-client.spec.ts`
- [ ] `rg -n "not found|private|transient|rename|transfer|order" tests/intake/preflight.spec.ts`
- [ ] `rg -n "retry|attempt|max|TRANSIENT_ERROR" tests/intake/github-client.spec.ts`
- [ ] `rg -n "gap|closure|npm exec|node -v|npm -v" .planning/phases/01-repository-intake-foundation/01-VALIDATION.md`
- [ ] `node -v && npm -v`
- [ ] Optional (non-blocking when vitest tooling is unavailable): `npm exec --yes vitest run tests/intake/github-client.spec.ts tests/intake/preflight.spec.ts`
</verification>

<success_criteria>
- All tasks completed
- All required verification checks pass
- Gap scenarios are directly represented in tests
- Verification steps remain runnable without pnpm dependency
</success_criteria>

<output>
After completion, update `.planning/phases/01-repository-intake-foundation/01-VALIDATION.md` and create `.planning/phases/01-repository-intake-foundation/01-04-SUMMARY.md`.
</output>
