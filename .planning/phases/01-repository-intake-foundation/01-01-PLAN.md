---
phase: 01-repository-intake-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/github-researcher/lib/intake/types.ts
  - skills/github-researcher/lib/intake/parser.ts
  - skills/github-researcher/lib/intake/normalizer.ts
  - skills/github-researcher/lib/intake/validator.ts
autonomous: true
requirements:
  - INPT-01
  - INPT-04
user_setup: []
must_haves:
  truths:
    - "GitHub URL and @owner/repo shorthand inputs are accepted for intake start."
    - "Malformed or unsupported target formats fail before analysis with structured input errors."
    - "Accepted inputs are normalized to a canonical owner/repo reference candidate with applied-normalization provenance."
  artifacts:
    - path: skills/github-researcher/lib/intake/parser.ts
      provides: URL and shorthand parsing for supported v1 forms
      min_lines: 40
      exports:
        - parseRepositoryTarget
    - path: skills/github-researcher/lib/intake/normalizer.ts
      provides: safe normalization pipeline for owner/repo candidates
      min_lines: 40
      exports:
        - normalizeRepositoryTarget
    - path: skills/github-researcher/lib/intake/validator.ts
      provides: host and structural validation with typed error output
      min_lines: 30
      exports:
        - validateRepositoryCandidate
  key_links:
    - from: skills/github-researcher/lib/intake/parser.ts
      to: skills/github-researcher/lib/intake/normalizer.ts
      via: parser output passed into normalization stage
      pattern: parseRepositoryTarget|normalizeRepositoryTarget
    - from: skills/github-researcher/lib/intake/normalizer.ts
      to: skills/github-researcher/lib/intake/validator.ts
      via: normalized target forwarded to policy validation
      pattern: validateRepositoryCandidate
---

<objective>
Implement the intake parser/normalizer/validator foundation for GitHub repository target handling.

Purpose: Make repository intake deterministic and fail-fast before any downstream analysis work.
Output: Typed intake domain models plus parser, normalization, and policy validation modules for v1-supported forms.
</objective>

<execution_context>
@/Users/eddy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/eddy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-intake-foundation/01-CONTEXT.md
@.planning/phases/01-repository-intake-foundation/01-RESEARCH.md
@skills/github-researcher/SKILL.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define intake contracts and error taxonomy</name>
  <files>skills/github-researcher/lib/intake/types.ts</files>
  <action>Create the shared TypeScript types for raw input, parsed candidate, normalized output, canonical identity mapping, and preflight error classes. Lock error classes to INPUT_ERROR, PERMISSION_OR_UNSUPPORTED, and TRANSIENT_ERROR so later phases can rely on stable classification.</action>
  <verify>test -f skills/github-researcher/lib/intake/types.ts</verify>
  <automated>pnpm vitest run tests/intake/parser-normalizer.spec.ts</automated>
  <done>types.ts exports all intake contracts required by parser/normalizer/validator and includes explicit error class enum/type.</done>
</task>

<task type="auto">
  <name>Task 2: Implement parser and safe normalization with provenance</name>
  <files>skills/github-researcher/lib/intake/parser.ts, skills/github-researcher/lib/intake/normalizer.ts</files>
  <action>Implement parsing for standard GitHub URL, .git URL, tree/blob URL, and @owner/repo shorthand. Implement normalization that trims whitespace, strips trailing slash and .git suffix, collapses tree/blob subpath URLs to repository root, and records normalizations_applied without silent mutation.</action>
  <verify>rg -n "normalizations_applied|parseRepositoryTarget|normalizeRepositoryTarget" skills/github-researcher/lib/intake/parser.ts skills/github-researcher/lib/intake/normalizer.ts</verify>
  <automated>pnpm vitest run tests/intake/parser-normalizer.spec.ts</automated>
  <done>Parser returns structured candidate data for all accepted forms, and normalizer emits canonical candidate plus a provenance list of transformations.</done>
</task>

<task type="auto">
  <name>Task 3: Add policy validator and fail-fast unsupported-host handling</name>
  <files>skills/github-researcher/lib/intake/validator.ts</files>
  <action>Implement validation that enforces github.com host, owner/repo segment integrity, and non-empty canonical candidate. Reject non-github hosts with actionable guidance and no auto-correction of suspected typos. Return typed structured failures aligned to the taxonomy.</action>
  <verify>rg -n "github\.com|INPUT_ERROR|PERMISSION_OR_UNSUPPORTED|validateRepositoryCandidate" skills/github-researcher/lib/intake/validator.ts</verify>
  <automated>pnpm vitest run tests/intake/validator.spec.ts</automated>
  <done>Validator emits stable structured results with error_class/error_code/message/details and clearly distinguishes input failures from unsupported target policies.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest run tests/intake/parser-normalizer.spec.ts` smoke check passes after each task edit
- [ ] `pnpm vitest run tests/intake/parser-normalizer.spec.ts tests/intake/validator.spec.ts` passes
- [ ] Parser accepts all v1 forms and rejects malformed targets with typed input errors
- [ ] Normalization provenance is preserved for every transformation
- [ ] Host-policy validation rejects non-`github.com` inputs with actionable guidance
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Intake foundation is reusable by preflight orchestration without format translation
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-intake-foundation/01-01-SUMMARY.md`
</output>
