---
phase: 02-unattended-execution-reliability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/github-researcher/lib/reliability/types.ts
  - skills/github-researcher/lib/reliability/state-machine.ts
  - skills/github-researcher/lib/reliability/retry-policy.ts
  - skills/github-researcher/lib/reliability/run-controller.ts
  - tests/reliability/state-machine.spec.ts
  - tests/reliability/retry-policy.spec.ts
autonomous: true
requirements:
  - RELY-01
  - RELY-03
  - RELY-05
user_setup: []
must_haves:
  truths:
    - "Run lifecycle is controlled by an explicit state machine with only completed/failed/cancelled as terminal states."
    - "Transient and rate-limit failures are retried automatically with bounded attempts and backoff+jitter."
    - "Retry exhaustion and hard-fail classifications force deterministic terminal failure with persisted failure context."
    - "Controller behavior is completion-seeking by default and only terminates early for explicit hard-fail/cancel criteria."
  artifacts:
    - path: skills/github-researcher/lib/reliability/state-machine.ts
      provides: allowed transition table and guard validation for run lifecycle
      min_lines: 60
      exports:
        - assertTransition
        - isTerminalState
    - path: skills/github-researcher/lib/reliability/retry-policy.ts
      provides: retry classification and bounded exponential backoff with jitter
      min_lines: 70
      exports:
        - classifyFailure
        - executeWithRetry
    - path: skills/github-researcher/lib/reliability/run-controller.ts
      provides: orchestration primitives that apply state + retry decisions toward terminal outcomes
      min_lines: 80
      exports:
        - createRunController
    - path: tests/reliability/state-machine.spec.ts
      provides: transition validity and terminal immutability assertions
      min_lines: 40
    - path: tests/reliability/retry-policy.spec.ts
      provides: retryable-class mapping, attempt ceilings, and exhaustion behavior checks
      min_lines: 50
  key_links:
    - from: skills/github-researcher/lib/reliability/run-controller.ts
      to: skills/github-researcher/lib/reliability/state-machine.ts
      via: controller delegates transition legality to state-machine guards
      pattern: assertTransition|isTerminalState
    - from: skills/github-researcher/lib/reliability/run-controller.ts
      to: skills/github-researcher/lib/reliability/retry-policy.ts
      via: stage execution wraps retryable operations through retry policy
      pattern: classifyFailure|executeWithRetry
    - from: tests/reliability/retry-policy.spec.ts
      to: skills/github-researcher/lib/reliability/retry-policy.ts
      via: test matrix validates transient, rate-limit, and hard-fail routing
      pattern: executeWithRetry|classifyFailure
---

<objective>
Implement the reliability core for unattended execution: run state machine, retry policy, and controller behavior.

Purpose: Establish deterministic, completion-oriented control flow that can drive runs unattended and fail predictably when retry boundaries are exhausted.
Output: Production-ready reliability core modules with unit tests proving RELY-01/03/05 semantics.
</objective>

<execution_context>
@/Users/eddy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/eddy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-unattended-execution-reliability/02-CONTEXT.md
@.planning/phases/02-unattended-execution-reliability/02-RESEARCH.md
@.planning/phases/02-unattended-execution-reliability/02-VALIDATION.md
@skills/github-researcher/SKILL.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define reliability contracts and state transition guards</name>
  <files>skills/github-researcher/lib/reliability/types.ts, skills/github-researcher/lib/reliability/state-machine.ts</files>
  <action>Create typed contracts for run states, terminal reasons, failure classes, and retry decisions. Implement allowed transition matrix for queued/running/retrying/paused/failed/completed/cancelled with guards that reject illegal transitions and prevent mutation after terminal states.</action>
  <verify>rg -n "RunState|TerminalReason|FailureClass|assertTransition|isTerminalState" skills/github-researcher/lib/reliability/types.ts skills/github-researcher/lib/reliability/state-machine.ts</verify>
  <automated>npm exec --yes vitest run tests/reliability/state-machine.spec.ts</automated>
  <done>State model is explicit, illegal transitions are blocked with deterministic errors, and terminal-state immutability is enforced by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded retry policy for transient and rate-limit failures</name>
  <files>skills/github-researcher/lib/reliability/retry-policy.ts, tests/reliability/retry-policy.spec.ts</files>
  <action>Implement retry classification that allows retries only for transient failures plus 429/rate-limit 403. Add executeWithRetry with maxAttempts=3, exponential backoff with jitter, delay cap, and exhaustion output containing terminal_reason=transient_exhausted plus preserved diagnostics.</action>
  <verify>rg -n "maxAttempts|429|403|transient_exhausted|executeWithRetry|classifyFailure" skills/github-researcher/lib/reliability/retry-policy.ts tests/reliability/retry-policy.spec.ts</verify>
  <automated>npm exec --yes vitest run tests/reliability/retry-policy.spec.ts</automated>
  <done>Retry policy enforces bounded attempts, classifies retryable vs non-retryable paths correctly, and exposes deterministic exhaustion results.</done>
</task>

<task type="auto">
  <name>Task 3: Build run controller core and completion-seeking orchestration tests</name>
  <files>skills/github-researcher/lib/reliability/run-controller.ts, tests/reliability/state-machine.spec.ts, tests/reliability/retry-policy.spec.ts</files>
  <action>Implement run controller primitives that apply transition guards and retry policy around stage execution. Ensure controller defaults to continue-toward-completion behavior unless hard_failure/dependency_blocked/cancel is reached, and persist failure context payload on terminal failure paths.</action>
  <verify>rg -n "createRunController|hard_failure|dependency_blocked|completed|failed|cancelled" skills/github-researcher/lib/reliability/run-controller.ts</verify>
  <automated>npm exec --yes vitest run tests/reliability/state-machine.spec.ts tests/reliability/retry-policy.spec.ts</automated>
  <done>Controller APIs implement completion-seeking behavior with deterministic terminal routing and are covered by reliability unit tests.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm exec --yes vitest run tests/reliability/state-machine.spec.ts` passes
- [ ] `npm exec --yes vitest run tests/reliability/retry-policy.spec.ts` passes
- [ ] Retry classification proves only transient + rate-limit classes are retried
- [ ] Attempt ceiling is enforced at 3 with terminal `transient_exhausted` behavior
- [ ] State machine enforces terminal immutability and disallows illegal transitions
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- RELY-01/RELY-03/RELY-05 behaviors are encoded in runtime contracts and tests
- Reliability core modules are ready for checkpoint/resume and progress integration in Wave 2
</success_criteria>

<output>
After completion, create `.planning/phases/02-unattended-execution-reliability/02-01-SUMMARY.md`
</output>
